# نقشه راه توسعه و استانداردسازی قالب مس کرمان

این سند شامل برنامه جامع توسعه، فازبندی پروژه، و جزئیات فنی جهت تبدیل قالب استاتیک به یک پوسته وردپرس تمام‌عیار و قابل تنظیم است.

---

## ۱. تحلیل و شناسایی ساختار قالب

### الف) بخش‌های سراسری (Global)
*   **رنگ‌ها (Colors):**
    *   `--color-copper`: رنگ اصلی (مسی)
    *   `--color-industrial-green`: سبز صنعتی
    *   `--color-bg-body`: پس‌زمینه
    *   `--color-text-main`: متن اصلی
    *   `--color-soft-gold`: طلایی ملایم
*   **تایپوگرافی:** فونت `IRANYekanX`.
*   **هدر (Header):**
    *   نوار بالایی (Top Bar): آدرس، ایمیل، شبکه‌های اجتماعی، تغییر زبان.
    *   نویگیشن اصلی: لوگو، منو، دکمه جستجو، دکمه موبایل.
*   **فوتر (Footer):**
    *   ستون ۱: لوگو و توضیحات کوتاه.
    *   ستون ۲: دسترسی سریع (منو).
    *   ستون ۳: بخش‌های شرکت (منو).
    *   ستون ۴: اطلاعات تماس و شبکه‌های اجتماعی.
    *   کپی‌رایت.

### ب) صفحه اصلی (Front Page)
1.  **Hero Section (اسلایدر):** عنوان، توضیحات، دو دکمه (CTA)، تصویر پس‌زمینه.
2.  **About Us (درباره ما):** عنوان، متن توضیحات، باکس ماموریت/چشم‌انداز، دکمه‌ها، تصویر، شمارنده تجربه.
3.  **Ads Section (آگهی‌ها):** فیلترها (مناقصه/مزایده)، کارت‌های آگهی (تصویر، عنوان، مهلت، وضعیت).
4.  **News Section (اخبار):** خبر ویژه (بزرگ)، لیست اخبار جانبی.
5.  **Icon Links (دسترسی سریع آیکون‌دار):** ۵ آیتم (گزارش مالی، ایمنی، فرم‌ها، غیره).
6.  **FAQ (سوالات متداول):** آکاردئون سوال و جواب.
7.  **Subsidiaries (شرکت‌های تابعه):** لیست لوگو و نام شرکت‌ها.
8.  **Contact (تماس):** اطلاعات تماس، فرم تماس (Ajax).

---

## ۲. اولویت‌بندی فازهای استانداردسازی

### فاز ۱: زیرساخت و استانداردسازی اولیه (Foundation)
*   **هدف:** تفکیک فایل‌ها و راه‌اندازی ساختار استاندارد وردپرس.
*   **اقدامات:**
    *   ایجاد `functions.php`.
    *   خرد کردن `index.php` به `header.php`، `footer.php` و `front-page.php`.
    *   استفاده از `wp_enqueue_scripts` برای لود کردن CSS/JS.
    *   ثبت منوها (`register_nav_menus`).

### فاز ۲: تنظیمات هویت و هدر (Header & Identity)
*   **هدف:** قابل تنظیم کردن لوگو، اطلاعات تماس هدر و شبکه‌های اجتماعی.
*   **بخش‌های Customizer:**
    *   `Identity`: لوگو، فاوآیکون (پیش‌فرض وردپرس).
    *   `Top Bar Settings`: آدرس، ایمیل، نمایش/عدم نمایش.
    *   `Social Media`: لینک‌های اینستاگرام، لینکدین، توییتر و...

### فاز ۳: تنظیمات بصری و رنگ‌ها (Visual & Colors)
*   **هدف:** کنترل متغیرهای CSS از طریق پنل.
*   **بخش‌های Customizer:**
    *   `Colors`: اتصال Color Picker به متغیرهای CSS (`:root`).
    *   `Typography`: انتخاب فونت (در صورت افزودن فونت‌های بیشتر) یا سایز پایه.

### فاز ۴: صفحه اصلی - بخش ۱ (Hero & About)
*   **هدف:** مدیریت اسلایدر و بخش درباره ما.
*   **بخش‌های Customizer:**
    *   `Hero Section`: تصویر پس‌زمینه، تیتر اصلی، تیتر فرعی، متن دکمه‌ها و لینک‌ها.
    *   `About Section`: تیتر، متن، تصویر کناری، متن ماموریت/چشم‌انداز.

### فاز ۵: صفحه اصلی - بخش ۲ (Dynamic Content)
*   **هدف:** اتصال بخش‌های آگهی‌ها و اخبار به کوئری‌های وردپرس.
*   **اقدامات:**
    *   ایجاد Custom Post Type برای "آگهی‌ها" (Ads) و "اخبار" (که همان Post عادی است).
    *   داینامیک کردن لوپ‌ها در `front-page.php`.

### فاز ۶: فوتر و ویجت‌ها (Footer & Widgets)
*   **هدف:** مدیریت ستون‌های فوتر.
*   **اقدامات:**
    *   تعریف `sidebar` (ناحیه ویجت) برای ستون‌های فوتر.
    *   تبدیل متن کپی‌رایت به تنظیمات Customizer.

---

## ۳. ساختار کدنویسی (Coding Standards)

ساختار پوشه `inc/` برای ماژولار بودن:

```text
inc/
  ├── customizer/
  │     ├── core/
  │     │     ├── class-customizer-base.php  (کلاس پایه)
  │     │     └── sanitization.php           (توابع امنیتی)
  │     ├── panels/
  │     │     ├── header-config.php
  │     │     ├── hero-config.php
  │     │     └── footer-config.php
  │     └── customizer-loader.php
  ├── theme-setup.php  (تنظیمات اولیه تم)
  └── enqueue.php      (فایل‌های استاتیک)
```

*   **Sanitization:** تمام ورودی‌ها باید با `sanitize_text_field`, `sanitize_hex_color`, `esc_url_raw` ایمن‌سازی شوند.
*   **Hooks:** استفاده از `customize_register`.

---

## ۴. معیار موفقیت (Success Metrics)
1.  **Zero Hardcode:** هیچ متن فارسی نباید در فایل‌های PHP باقی بماند (همه چیز یا از دیتابیس یا از فایل زبان).
2.  **Live Preview:** تغییر رنگ‌ها و متون باید بدون رفرش صفحه (postMessage) در کاستومایزر دیده شود.
3.  **W3C Valid:** خروجی HTML باید معتبر باشد.

---

## ۵. مستندسازی (Documentation)
*   ایجاد فایل `README.md` با دستورالعمل نصب.
*   کامنت‌گذاری کدها طبق استاندارد PHPDoc.
