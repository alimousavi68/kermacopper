# برنامه جامع انتقال و توسعه قالب وردپرس (پروژه مس)

این سند برنامه گام‌به‌گام برای انتقال طراحی از پروژه React (رفرنس) به قالب وردپرس بر پایه فریم‌ورک Hasht را تشریح می‌کند.

## فاز ۱: زیرساخت و پیکربندی اولیه (Foundation)
**هدف:** آماده‌سازی محیط توسعه و انتقال دارایی‌های ثابت (Assets).
- [ ] **بررسی و انتقال فونت‌ها**: انتقال فونت `IRANYekanX` از پوشه رفرنس به قالب وردپرس.
- [ ] **پیکربندی Tailwind CSS**:
    - تنظیم رنگ‌های سازمانی (`copper: #c4a962`, `industrial-green: #0B6E60`, `soft-gold`) در `tailwind.config.js`.
    - تنظیم فونت‌فمیلی پیش‌فرض.
- [ ] **انتقال تصاویر**: کپی تصاویر موجود در پوشه `images` رفرنس به پوشه `assets/images` قالب.
- [ ] **نصب آیکون‌ها**: تصمیم‌گیری و نصب کتابخانه آیکون (Lucide - نسخه وب یا SVG).

## فاز ۲: لایوت اصلی (Global Layout)
**هدف:** پیاده‌سازی هدر و فوتر که در تمام صفحات مشترک هستند.
- [ ] **هدر (Header)**:
    - نوار ابزار بالا (Top Bar) شامل آدرس، ایمیل و شبکه‌های اجتماعی.
    - نویگیشن اصلی (Desktop Menu) با افکت‌های هاور.
    - منوی موبایل (Mobile Menu) و ریسپانسیو.
    - **داینامیک‌سازی**: افزودن تنظیمات کاستومایزر برای لینک‌های اجتماعی و اطلاعات تماس.
- [ ] **فوتر (Footer)**:
    - پیاده‌سازی ۴ ستون اصلی.
    - کپی‌رایت و لینک‌های پایین صفحه.
    - **داینامیک‌سازی**: استفاده از ابزارک‌ها (Widgets) یا تنظیمات کاستومایزر برای ستون‌ها.

## فاز ۳: صفحه اصلی - بخش به بخش (Homepage)
**هدف:** پیاده‌سازی سکشن‌های صفحه اصلی به صورت ماژولار (`views/partials/`).

### ۳.۱. هیرو سکشن (Hero Section)
- [ ] طراحی استاتیک اسلایدر/بنر اصلی.
- [ ] **داینامیک‌سازی**: ایجاد تنظیمات کاستومایزر برای تغییر تصویر پس‌زمینه، تیتر و متن دکمه‌ها.

### ۳.۲. درباره ما (About Us)
- [ ] پیاده‌سازی ساختار دو ستونه (متن + شبکه تصاویر).
- [ ] **داینامیک‌سازی**: امکان تغییر متن و تصاویر از طریق برگه "درباره ما" یا تنظیمات پوسته.

### ۳.۳. آگهی‌ها (Ads Section) - **نیاز به توسعه Backend**
- [ ] طراحی کارت‌های مناقصه/مزایده.
- [ ] **Backend**: ایجاد Custom Post Type به نام `tender` (مناقصات/مزایدات).
- [ ] **Logic**: کوئری گرفتن از آخرین آگهی‌ها و نمایش فیلتر (JS Filter یا AJAX).

### ۳.۴. اخبار (News Section)
- [ ] طراحی کارت‌های خبر.
- [ ] **Logic**: نمایش آخرین پست‌های بلاگ وردپرس (Standard Posts).

### ۳.۵. لینک‌های دسترسی سریع (Icon Links)
- [ ] پیاده‌سازی نوار آیکون‌ها.
- [ ] **داینامیک‌سازی**: ایجاد آرایه تنظیمات یا منوی فهرست‌ها.

### ۳.۶. گالری پروژه‌ها (Projects) - **نیاز به توسعه Backend**
- [ ] طراحی بخش پروژه‌های ویژه و لیست کناری.
- [ ] **Backend**: ایجاد Custom Post Type به نام `project`.

### ۳.۷. تماس با ما (Contact Section)
- [ ] طراحی فرم و اطلاعات تماس.
- [ ] **Logic**: اتصال فرم به Contact Form 7.

## فاز ۴: نهایی‌سازی و بهینه‌سازی
- [ ] بررسی ریسپانسیو در تمام دستگاه‌ها.
- [ ] بهینه‌سازی سرعت (Lighthouse Check).
- [ ] تست نهایی عملکرد تمام دکمه‌ها و لینک‌ها.
